# Flask Authentication Application

Веб-приложение на Flask с системой аутентификации пользователей. Проект реализует регистрацию, вход, выход, защищенные страницы, редактирование профиля и функционал анкеты для авторизованных пользователей.

## 📋 Содержание

- [Описание](#-описание)
- [Функциональность](#-функциональность)
- [Технологии](#-технологии)
- [Структура проекта](#-структура-проекта)
- [Установка и настройка](#-установка-и-настройка)
- [Использование](#-использование)
- [API и маршруты](#-api-и-маршруты)
- [База данных](#-база-данных)
- Конфигурация
- [Безопасность](#-безопасность)
- [Разработка](#-разработка)

## 📝 Описание

Это веб-приложение на Flask, которое демонстрирует базовую систему аутентификации и работу с формами. Пользователи могут регистрироваться, входить в систему, выходить и получать доступ к защищенным страницам. Пароли хранятся в зашифрованном виде с использованием bcrypt. Реализована возможность редактирования профиля (изменение имени, email и пароля). Дополнительно реализован функционал анкеты, где пользователи могут ввести свои данные и получить расчетный год рождения.

## ✨ Функциональность

### Аутентификация

- **Регистрация пользователей** - создание нового аккаунта с валидацией данных
- **Вход в систему** - аутентификация существующих пользователей
- **Выход из системы** - завершение сессии пользователя
- **Защищенные страницы** - доступ к личному кабинету только для авторизованных пользователей
- **Редактирование профиля** - возможность изменения имени пользователя, email и пароля
- **Сессии пользователей** - поддержка "Запомни меня" при входе

### Валидация и безопасность

- **Валидация форм** - проверка уникальности email и username
- **Хеширование паролей** - безопасное хранение паролей с использованием bcrypt
- **CSRF защита** - автоматическая защита всех форм от CSRF атак

### Дополнительные функции

- **Анкета пользователя** - форма для ввода личных данных (имя, город, хобби, возраст) с автоматическим расчетом года рождения
- **Отображение результатов** - красивое отображение введенных данных в виде карточки Bootstrap

## 🛠 Технологии

- **Flask 3.0.0** - веб-фреймворк для Python
- **Flask-SQLAlchemy 3.1.1** - ORM для работы с базой данных
- **Flask-Bcrypt 1.0.1** - хеширование паролей
- **Flask-Login 0.6.3** - управление сессиями пользователей
- **Flask-WTF 1.2.1** - работа с формами и защита от CSRF
- **WTForms 3.1.1** - валидация форм
- **SQLite** - база данных (по умолчанию)
- **Bootstrap 5.3.0** - CSS-фреймворк для стилизации интерфейса

## 📁 Структура проекта

```text
VD07-app-flask-auth/
│
├── app/                    # Основное приложение (Python Package)
│   ├── __init__.py        # Инициализация Flask приложения и расширений
│   ├── models.py          # Модели базы данных (User)
│   ├── routes.py          # Маршруты и обработчики запросов
│   ├── forms.py           # Формы (RegistrationForm, LoginForm, QuestionnaireForm, UpdateProfileForm)
│   └── templates/         # HTML шаблоны (Jinja2)
│       ├── base.html      # Базовый шаблон с навигацией
│       ├── home.html      # Главная страница
│       ├── login.html     # Страница входа
│       ├── register.html  # Страница регистрации
│       ├── account.html   # Личный кабинет (требует авторизации)
│       └── questionnaire.html  # Страница анкеты
│
├── instance/              # Папка для базы данных (создается автоматически)
│   └── site.db           # SQLite база данных
│
├── main.py               # Точка входа приложения
├── create_db.py          # Скрипт для создания базы данных
├── config.py             # Конфигурация приложения (класс Config)
├── requirements.txt      # Зависимости проекта
├── .gitignore           # Игнорируемые файлы для Git
└── README.md            # Документация проекта
```

## 🚀 Установка и настройка

### Предварительные требования

- Python 3.8 или выше
- pip (менеджер пакетов Python)
- Git (для клонирования репозитория)

### Шаги установки

1. **Клонируйте репозиторий:**

   ```bash
   git clone <repository-url>
   cd VD07-app-flask-auth
   ```

   Или скачайте архив и распакуйте в нужную директорию.

2. **Создайте виртуальное окружение (рекомендуется):**

   ```bash
   python -m venv venv
   ```

3. **Активируйте виртуальное окружение:**

   На Windows:

   ```bash
   venv\Scripts\activate
   ```

   На Linux/Mac:

   ```bash
   source venv/bin/activate
   ```

4. **Установите зависимости:**

   ```bash
   pip install -r requirements.txt
   ```

5. **Создайте базу данных:**

   ```bash
   python create_db.py
   ```

   Или база данных будет создана автоматически при первом запуске приложения.

6. **Настройте конфигурацию (см. раздел "Конфигурация" ниже)**

## 💻 Использование

### Запуск приложения

```bash
python main.py
```

Приложение будет доступно по адресу: `http://127.0.0.1:5000`

### Режим отладки

Приложение запускается в режиме отладки (`debug=True`), что позволяет:

- Видеть подробные ошибки в браузере
- Автоматически перезагружать приложение при изменении кода

**⚠️ Внимание:** Не используйте режим отладки в продакшене!

### Использование приложения

1. **Регистрация:**
   - Перейдите на `/register` или нажмите "Register" в навигационном меню
   - Заполните форму:
     - Username (минимум 2 символа, максимум 35)
     - Email (должен быть валидным и уникальным)
     - Password
     - Confirm Password (должен совпадать с паролем)
   - После успешной регистрации вы будете перенаправлены на страницу входа

2. **Вход:**
   - Перейдите на `/login` или нажмите "Login" в навигационном меню
   - Введите email и пароль
   - Опционально: отметьте "Запомни меня" для сохранения сессии
   - После успешного входа вы будете перенаправлены на главную страницу

3. **Анкета:**
   - Перейдите на `/questionnaire` или нажмите "Анкета" в навигационном меню
   - Заполните форму:
     - Ваше имя
     - Город
     - Любимое хобби
     - Возраст (от 0 до 120)
   - Нажмите "Отправить"
   - Под формой появится карточка с результатами, включая расчетный год рождения

4. **Личный кабинет и редактирование профиля:**
   - После входа перейдите на `/account` или нажмите "Account" в навигационном меню
   - Эта страница доступна только для авторизованных пользователей
   - На странице аккаунта вы можете:
     - Изменить имя пользователя
     - Изменить email (с проверкой уникальности)
     - Изменить пароль (опционально, можно оставить пустым)
   - После внесения изменений нажмите "Сохранить изменения"

5. **Выход:**
   - Нажмите "Logout" в навигационном меню

## 🛣 API и маршруты

| Маршрут | Метод | Описание | Требуется авторизация |
|---------|-------|----------|----------------------|
| `/` | GET | Главная страница | Нет |
| `/home` | GET | Главная страница (алиас) | Нет |
| `/register` | GET, POST | Страница регистрации | Нет |
| `/login` | GET, POST | Страница входа | Нет |
| `/logout` | GET | Выход из системы | Нет |
| `/questionnaire` | GET, POST | Страница анкеты | Нет |
| `/account` | GET, POST | Личный кабинет и редактирование профиля | Да |

### Защищенные маршруты

Маршрут `/account` защищен декоратором `@login_required`. При попытке доступа неавторизованным пользователем происходит перенаправление на страницу входа (`/login`). На этой странице пользователи могут редактировать свой профиль: изменять имя пользователя, email и пароль.

## 🗄 База данных

### Модель User

База данных содержит одну таблицу `user` со следующими полями:

- `id` (Integer, Primary Key) - уникальный идентификатор пользователя
- `username` (String(20), Unique, Not Null) - имя пользователя
- `email` (String(120), Unique, Not Null) - электронная почта
- `password` (String(60), Not Null) - хешированный пароль (bcrypt)

### Создание базы данных

База данных создается автоматически при первом запуске приложения или при выполнении скрипта:

```bash
python create_db.py
```

### Расположение базы данных

По умолчанию база данных SQLite создается в папке `instance/site.db`. Эта папка автоматически создается Flask-SQLAlchemy.

## ⚙️ Конфигурация

Конфигурация приложения находится в файле `config.py`. Используется класс `Config` с поддержкой переменных окружения.

### Настройка SECRET_KEY

**Важно для безопасности!** Обязательно измените `SECRET_KEY` перед использованием в продакшене.

#### Способ 1: Через переменные окружения (рекомендуется)

Создайте файл `.env` в корне проекта (не коммитьте его в Git):

```bash
SECRET_KEY=ваш-случайный-секретный-ключ-здесь
DATABASE_URL=sqlite:///site.db
```

Или установите переменные окружения в системе:

**Windows (PowerShell):**

```powershell
$env:SECRET_KEY="ваш-случайный-секретный-ключ"
```

**Linux/Mac:**

```bash
export SECRET_KEY="ваш-случайный-секретный-ключ"
```

#### Способ 2: Прямое редактирование config.py

Откройте файл `config.py` и замените значение по умолчанию:

```python
SECRET_KEY = os.environ.get('SECRET_KEY') or 'ваш-случайный-секретный-ключ-здесь'
```

### Генерация безопасного ключа

Для генерации безопасного ключа используйте Python:

```python
import secrets
print(secrets.token_hex(16))
```

Или через командную строку:

```bash
python -c "import secrets; print(secrets.token_hex(16))"
```

### Настройка базы данных

По умолчанию используется SQLite. Для использования другой БД измените `SQLALCHEMY_DATABASE_URI` в `config.py` или через переменную окружения `DATABASE_URL`.

Пример для PostgreSQL:

```python
SQLALCHEMY_DATABASE_URI = os.environ.get('DATABASE_URL') or 'postgresql://user:password@localhost/dbname'
```

## 🔒 Безопасность

### Реализованные меры безопасности

1. **Хеширование паролей** - пароли хранятся в виде bcrypt хешей, а не в открытом виде
2. **CSRF защита** - Flask-WTF автоматически защищает все формы от CSRF атак
3. **Валидация данных** - проверка уникальности email и username на уровне формы
4. **Защита маршрутов** - использование `@login_required` для ограничения доступа к защищенным страницам
5. **Конфигурация через переменные окружения** - поддержка безопасного хранения секретных ключей

### Рекомендации по безопасности

1. **SECRET_KEY** - обязательно измените `SECRET_KEY` на случайную строку перед продакшеном
2. **Переменные окружения** - используйте переменные окружения для хранения секретных ключей в продакшене
3. **HTTPS** - используйте HTTPS в продакшене для защиты данных при передаче
4. **База данных** - для продакшена рассмотрите использование PostgreSQL или MySQL вместо SQLite
5. **Rate Limiting** - добавьте ограничение на количество попыток входа для защиты от брутфорса
6. **Валидация паролей** - добавьте требования к сложности пароля (минимальная длина, наличие цифр и символов)
7. **Логирование** - добавьте логирование важных событий (попытки входа, ошибки)

## 🛠 Разработка

### Архитектура проекта

Проект следует стандартной структуре Flask приложений:

- **app/** - основной пакет приложения
  - `__init__.py` - инициализация приложения и расширений
  - `models.py` - модели базы данных
  - `routes.py` - маршруты и обработчики запросов
  - `forms.py` - формы Flask-WTF
  - `templates/` - HTML шаблоны Jinja2
- **config.py** - конфигурация приложения
- **main.py** - точка входа

### Добавление новых функций

1. **Новый маршрут:** Добавьте функцию в `app/routes.py` с декоратором `@app.route()`
2. **Новая форма:** Создайте класс формы в `app/forms.py`, наследуясь от `FlaskForm`
3. **Новый шаблон:** Создайте HTML файл в `app/templates/`, наследуясь от `base.html`

### Известные ограничения

- Приложение использует SQLite, что подходит для разработки, но не для высоконагруженных продакшен-систем
- Режим отладки включен по умолчанию (отключите в продакшене)
- Нет обработки ошибок базы данных (добавьте try-except блоки)
- Нет валидации сложности паролей

### Рекомендации по улучшению

1. **Обработка ошибок** - добавить обработку ошибок базы данных и других исключений
2. **Email подтверждение** - добавить подтверждение email при регистрации
3. **Восстановление пароля** - реализовать функционал восстановления пароля
4. **Профиль пользователя** - расширить страницу аккаунта информацией о пользователе
5. **Стилизация** - улучшить CSS для более современного внешнего вида
6. **Логирование** - добавить логирование важных событий
7. **Тестирование** - написать unit-тесты для основных функций
8. **API** - добавить REST API для интеграции с другими приложениями

## 📝 Лицензия

Этот проект создан в образовательных целях.

## 👤 Автор

**Георгий Белянин (Georgy Belyanin)**

- Email: [georgy.belyanin@gmail.com](mailto:georgy.belyanin@gmail.com)

Проект создан для демонстрации базовой системы аутентификации на Flask.

---

**Примечание:** Это базовое приложение для обучения. Для продакшена необходимо добавить дополнительные меры безопасности и функциональность, как указано в разделах "Безопасность" и "Рекомендации по улучшению".
